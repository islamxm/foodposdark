import { useBaseStatus } from "@/shared/lib/hook/useBaseStatus"

export const useDeleteOrderItem = () => {
  const {
    isLoading,
    isError,
    isSuccess
  } = useBaseStatus()

  

  return {

  }
}